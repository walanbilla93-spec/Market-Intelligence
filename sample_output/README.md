# Sample only

These two rows illustrate the causal timestamp and schema shape. They are not live observations and must not be used for research. A real run writes hourly files under `MI_DATA_DIR/exports` and generates a SHA-256 manifest with `orayan-market-intel export`.
